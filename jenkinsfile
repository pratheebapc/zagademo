pipeline{
    agent any
    stages{
        stage("package"){
            steps{
               sh "mvn clean package"
            }
        }
        stage("install"){
            steps{
                sh "mvn clean install"
            }
        }
    }
}